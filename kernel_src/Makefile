# This is only used by 2.5 builds hence it is not automatically generated
# It is just so kbuild what sub-directories have to be examined.

CONFIG_VMR_CORE=m
CONFIG_VMR_BENCH=m
CONFIG_VMR_SENSE=m
CONFIG_VMR_TEMPLATE=m
CONFIG_VMR_TEST=m

obj-$(CONFIG_VMR_CORE) += core/ 
obj-$(CONFIG_VMR_BENCH) += bench/
obj-$(CONFIG_VMR_SENSE) += sense/
obj-$(CONFIG_VMR_TEMPLATE) += template/
obj-$(CONFIG_VMR_TEST) += test/

